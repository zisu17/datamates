dbt-core==1.12.0

# 기본 타깃(local)이 DuckLake 다. duckdb 핀은 datamates/requirements.txt 와 **같아야**
# 한다 — 같은 카탈로그를 dbt 가 쓰고 앱이 읽으므로 버전이 갈리면 스냅샷 해석이 어긋난다.
dbt-duckdb==1.11.0
duckdb==1.5.5

elementary-data==0.25.1

# 롤백 타깃(DBT_TARGET=spark_local) 전용. 컨테이너 이미지에서는 뺐고 호스트에만 남긴다.
dbt-spark==1.11.0
pyspark==4.0.4
