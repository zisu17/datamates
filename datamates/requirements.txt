fastapi==0.121.2
uvicorn[standard]==0.41.0
pydantic==2.13.3
httpx==0.28.1
# 파일 업로드(데이터 수집)의 multipart 파싱. FastAPI 가 UploadFile 에 요구한다.
python-multipart==0.0.20
ruamel.yaml==0.18.16

# 데이터 수집의 적재 엔진. Spark 를 띄우지 않고 Iceberg REST 카탈로그에 직접 쓴다
# (dbt-spark 경로는 호출마다 JVM 기동에 약 15초가 고정으로 든다).
pyiceberg==0.11.1
pyarrow==25.0.0

# 미리보기·이력 조회의 읽기 엔진(warehouse.py). iceberg 확장으로 카탈로그에 직접
# 붙어 dbt show 의 17초를 0.035초로 줄인다. 없으면 화면의 이력 패널이 통째로
# 503 이 되므로 선택 의존성이 아니다.
duckdb==1.5.5

# 컬럼 계보(collineage.py)가 모델 SQL 을 파싱하는 데 쓴다.
# dbt 쪽 requirements 가 아니라 여기 있어야 한다 — 컨테이너에서는 dbt 와 앱이 서로 다른
# venv 에 들어가고(각각 /opt/dbt-venv 와 /opt/datamates-venv), 이걸 import 하는 것은
# 앱 프로세스다. 저쪽에 두면 앱 이미지에 설치되지 않아 계보 API 만 ImportError 로 죽는다.
sqlglot==30.14.0
