fastapi==0.121.2
uvicorn[standard]==0.41.0
pydantic==2.13.3
httpx==0.28.1
# 파일 업로드(데이터 수집)의 multipart 파싱. FastAPI 가 UploadFile 에 요구한다.
python-multipart==0.0.20
ruamel.yaml==0.18.16

# 데이터 수집이 원본을 Arrow 테이블로 만들어 DuckDB에 넘긴다.
pyarrow==25.0.0

# 웨어하우스 엔진. 세 가지 일을 한다 — 미리보기·이력 조회(warehouse.py),
# 데이터 수집의 적재(ingest.py), 그리고 dbt 변환(dbt-duckdb, 별도 설치).
# ducklake 확장으로 카탈로그에 붙는다. 없으면 화면이 통째로 503 이 되므로
# 선택 의존성이 아니다. docker/superset/requirements-local.txt 와 **같은 핀**이어야 한다.
duckdb==1.5.5

# 컬럼 계보(collineage.py)가 모델 SQL 을 파싱하는 데 쓴다.
# dbt 쪽 requirements 가 아니라 여기 있어야 한다 — 컨테이너에서는 dbt 와 앱이 서로 다른
# venv 에 들어가고(각각 /opt/dbt-venv 와 /opt/datamates-venv), 이걸 import 하는 것은
# 앱 프로세스다. 저쪽에 두면 앱 이미지에 설치되지 않아 계보 API 만 ImportError 로 죽는다.
sqlglot==30.14.0

# PostgreSQL 메타스토어 드라이버.
psycopg[binary]==3.2.10
