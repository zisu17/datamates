/* ── b16 — ── b16 — 상세 패널이 돌아왔으므로 툴바 여유폭 계산에 반영한다 ── (index.html 블록에서 기계적 추출, 동작 불변) */

/* 상세 패널이 돌아왔으므로 툴바 여유폭 계산에 반영한다 */
/* (applyTierDefaults — 덧붙이던 S.mPanelOpen = true 한 줄을 b01 본체로 옮겼다. 제거) */
S.mPanelOpen = true;
