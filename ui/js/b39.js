/* ── b39 — ── b39 — ── v2.6.1 — 관계도를 전부 비워도 기본 구성이 다시 생기지 않게 ── (index.html 블록에서 기계적 추출, 동작 불변) */

/* ── v2.6.1 — 관계도를 전부 비워도 기본 구성이 다시 생기지 않게 ─────────
   seedCanvas 는 S.nodes 가 비어 있으면 기본 8개를 다시 심는다.
   최초 1회만 심고, 사용자가 비운 상태는 그대로 존중한다.          */
S.seededOnce = S.seededOnce || S.nodes.length > 0;
