/* ── b26 — ── b26 — v2.4 — 컬럼이 수백 개인 테이블도 ERD 에서 다룰 수 있게 ── (index.html 블록에서 기계적 추출, 동작 불변) */

/* ============================================================
   v2.4 — 컬럼이 수백 개인 테이블도 ERD 에서 다룰 수 있게
   · 기본은 키 + 주요 만. 상자마다 전체 펼치기 / 컬럼 검색
   · 펼쳐도 상자 높이는 고정, 안쪽만 스크롤 → 관계선이 무너지지 않는다
   · 연결선 라벨의 의미를 연결 키 로 분명히 한다
   ============================================================ */
Object.assign(S, { erdCols: 'main', erdOpen: {}, erdQ: {} });
          /* 펼쳤을 때 컬럼 목록 최대 높이 */
           /* 상자 하나에 그리는 최대 줄 수 — 그 이상은 검색으로 */
            /* 주요 모드에서 보여줄 최대 줄 수 */
             /* 컬럼 검색줄 높이 */

/* 무엇을 보여줄지 — 키는 어떤 모드에서도 빠지지 않는다 */
/* entH 는 const 로 선언돼 있어 재할당할 수 없다 — 새 이름을 쓰고 호출부를 바꾼다 */
