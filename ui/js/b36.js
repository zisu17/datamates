/* ── b36 — ── b36 — 파이프라인 화면 — 구성 | 실행 흐름 | 실행 설정 ── (index.html 블록에서 기계적 추출, 동작 불변) */

/* 파이프라인 화면 — 구성 | 실행 흐름 | 실행 설정 */
/* (pagePipeDetail — 이후 정의가 값 캡처 없이 전면 교체. 죽은 층 제거) */
/* 구성 화면의 왼쪽 카탈로그 */
/* (pipeCatalog — 이후 정의가 값 캡처 없이 전면 교체. 죽은 층 제거) */
/* 모델링 → 파이프라인 이동 시 해당 카드를 골라 준다 */
/* (usePipeline — 이후 정의가 값 캡처 없이 전면 교체. 죽은 층 제거) */
/* 정의 화면 파이프라인에서 사용 도 같은 흐름 */
S.pipeNode = null;
