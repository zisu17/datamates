/* ── b21 — ── b21 — v2.3 — 모델링 관계 를 ERD 로, 파이프라인 흐름을 모델 카드 형태로 ── (index.html 블록에서 기계적 추출, 동작 불변) */

/* ============================================================
   v2.3 — 모델링 관계 를 ERD 로, 파이프라인 흐름을 모델 카드 형태로
   ============================================================ */
const EW = 266, ERH = 24, EHH = 46, EFH = 22;
/* 기본키 · 외래키 추정 — 물리 컬럼명 규칙을 그대로 쓴다 */
/* 관계선 — 입력 관계를 조인 키까지 함께 보여준다 */
/* 원천 → 정제 → 분석 순으로 열을 나눈다 */
/* 관계선 — 오른쪽 → 왼쪽, 끝에 1 / N 표기 */
/* 박스 이동 · 배경 패닝 */
