/* ── b28 — ── b28 — 배치·배율 계산도 실제 상자 높이를 쓰도록 맞춘다 ── (index.html 블록에서 기계적 추출, 동작 불변) */

/* 배치·배율 계산도 실제 상자 높이를 쓰도록 맞춘다 */
/* (erdZoomFit — 이후 정의가 값 캡처 없이 전면 교체. 죽은 층 제거) */
/* 컬럼 표시 방식이 바뀌면 상자 높이가 달라지므로 세로 간격을 다시 잡는다 */
