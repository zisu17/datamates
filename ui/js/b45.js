/* ── b45 — ── b45 — v2.9.1 — 자잘한 결함 정리 ── (index.html 블록에서 기계적 추출, 동작 불변) */

/* ============================================================
   v2.9.1 — 자잘한 결함 정리
   · 파이프라인 하단 상세 높이 조절이 안 되던 문제 (grip 미연결)
   · 관계도(ERD)에 카탈로그를 끌어다 놓을 수 없던 문제 (drop 미연결)
   · 하단 패널 접기/펼치기·높이 한계를 모델링 화면과 통일
   ============================================================ */
S.pdockMin = S.pdockMin || false;
const DOCK_MIN = 120, DOCK_MAX = 440;

/* 1) 하단 상세 — 높이 조절 + 접기 */
/* (pipeDock — 살아남던 접기 버튼·min 클래스는 b35 의 pipeDockChrome 으로 옮겼다.
   높이와 그립은 v2.9.2 가 매번 다시 써서 여기 것이 남지 않았다. 제거) */
/* 2) 관계도 — 카탈로그에서 끌어다 놓기 */
/* 3) 모델링 하단 패널도 같은 한계값을 쓴다 */
/* (wireGrips — 도크 그립을 통째로 다시 연결하던 층. b03 본체가 처음부터
   이 한계값(DOCK_MIN/MAX)과 transition 복원으로 연결하도록 합쳤다. 제거) */
