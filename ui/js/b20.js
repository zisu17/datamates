/* ── b20 — ── b20 — ── v2.2.1 — 관계 보기에서는 오른쪽 상세 패널을 두지 않는다 ── (index.html 블록에서 기계적 추출, 동작 불변) */

/* ── v2.2.1 — 관계 보기에서는 오른쪽 상세 패널을 두지 않는다 ─────────────
   관계 화면은 전체 의존 관계를 훑어보는 용도이므로 캔버스를 최대한 넓게 쓴다.
   개별 모델의 상세·편집은 정의 화면 하나로만 들어간다.            */
mpView = function () { return document.createDocumentFragment(); };
S.mPanelOpen = false;

/* 카드 메뉴 · 더블클릭으로 정의 화면으로 넘어간다 */
/* 관계 화면 안내와 더보기에서 상세 패널 항목을 뺀다 */
/* (canvasView — 이후 정의가 값 캡처 없이 전면 교체. 죽은 층 제거) */

/* (refreshPanel — 이후 정의가 값 캡처 없이 전면 교체. 죽은 층 제거) */
