/* ── b27 — ── b27 — ── 연결선 라벨 = 연결 키. 어떤 컬럼으로 이어지는지를 뜻한다 ── (index.html 블록에서 기계적 추출, 동작 불변) */

/* ── 연결선 라벨 = 연결 키. 어떤 컬럼으로 이어지는지를 뜻한다 ── */
/* ── ERD 머리말: 컬럼 표시 방식 + 라벨 설명 ── */
/* 상자 안 컬럼 목록 위에서는 휠이 목록을 스크롤한다 */
/* (wireZoomPan — [data-colscroll] 을 만드는 곳이 v5.7 계보 화면으로 바뀌며 사라졌다.
   조건이 참이 되는 순간이 없어 도달 불가. 제거) */
/* 입력·버튼을 누를 때는 상자를 끌지 않는다 */
HELP.modeling.items[4] = 'ERD 상자를 두 번 누르면 정의로, 선을 두 번 누르면 연결 설정으로 갑니다. 선 위의 이름은 두 모델을 잇는 연결 키입니다.';
HELP.modeling.items.splice(5, 0, '컬럼이 많은 모델은 컬럼: 키만 / 주요 / 전체 로 표시량을 조절하고, 상자 안에서 컬럼을 검색할 수 있습니다.');
