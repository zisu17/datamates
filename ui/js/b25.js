/* ── b25 — ── b25 — ERD — 배율 버튼도 화면 중앙을 기준으로 조정한다 ── (index.html 블록에서 기계적 추출, 동작 불변) */

/* ERD — 배율 버튼도 화면 중앙을 기준으로 조정한다 */
/* (erdView — 이후 정의가 값 캡처 없이 전면 교체. 죽은 층 제거) */
/* ── 파이프라인 실행 흐름 — ERD 와 같은 확대·축소 / 패닝 ── */
