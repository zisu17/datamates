/* ── b07 — ── b07 — 2. 데이터 카탈로그 — 워크 스페이스 중심 ── (index.html 블록에서 기계적 추출, 동작 불변) */

/* ============================================================
   2. 데이터 카탈로그 — 워크 스페이스 중심
   ============================================================ */
/* ── 오른쪽 메타데이터 패널 ── */
/* ── 워크 스페이스 관리 (제거) ──
   wsCreateModal 은 홈의 워크 스페이스 탭에서만 열렸다. 만든 묶음은 WS_USER 배열에만
   담겨 새로고침하면 사라졌다 — 서버에 저장하는 곳이 없었다. 탭과 함께 걷어낸다. */
