packages:
  - package: dbt-labs/dbt_utils
    version: [">=1.3.0", "<2.0.0"]

  # 테스트/실행 결과를 테이블에 적재한다. edr CLI 가 이 테이블을 읽어 리포트를 만든다.
  - package: elementary-data/elementary
    version: [">=0.25.0", "<0.26.0"]
